#include "main.h"

void interactiv(char *list_arg)
{
	char *prompt = "$ ", **arr = NULL, *buff = NULL, *lineptr = NULL;
	size_t buf_size = 0;
	int  i = 1, status = 0;

	while (1)
	{
		_putchar(prompt)
	}
}
